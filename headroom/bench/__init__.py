"""headroom-bench — single-command benchmark suite for Headroom.

Measures token reduction, answer fidelity, reversibility, latency,
coverage, and robustness across compression tools and content types.

    python -m headroom.bench run --suite all
"""

from __future__ import annotations
